@model IEnumerable<NewsPortal.Logic.Models.Post>

@{
    Layout = "_LayoutWithoutNavbar";
}

<div>
    @foreach (var post in Model)
    {
        <div class="form-horizontal">
            <hr />
            <div class="form-group">
                <h4>@Html.ActionLink(post.Name, "GetPost", "Post", post)</h4>
            </div>
            <div class="form-group">
                @post.Description
            </div>
        </div>
    }
</div>
